export interface Ingredients {
  mayo: boolean
  mustard: boolean
  ketchup: boolean
}
export interface Selection {
  id: string
  label: string
  price: number
}
export interface Badges {
  allIngredients: string
  none: string
}
